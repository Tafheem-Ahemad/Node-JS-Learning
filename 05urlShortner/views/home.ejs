<!DOCTYPE html>
<html lang="en">
<head>
	<style>
		body{
			font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
		}
	</style>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Home Page</title>
</head>
<body>
	<h1>HomePage</h1>
	<div>
		<% if (locals.id) { %>
			<p>Generated URL: http://localhost:8000/url/<%= id %> </p>
		<% } %>
		<form method="post" action="url/">
			<label>Enter your original UR</label>
			<input type="text" name="url" placeholder="Type your URL..">
			<button type="submit">Generate</button>
		</form>


		<div>
			<% if (locals.urls) { %>
				<table>
					<thead>
						<th>Sl No.</th>
						<th>Short URL</th>
						<th>Original URL</th>
						<th>Visited Count</th>
					</thead>
					<tbody>
						<% urls.forEach((ele,index) => { %>
							<tr>
								<td> <%= index+1 %> </td>
								<td> http://localhost:8000/url/<%= ele.shortID %> </td>
								<td> <%= ele.redirectURL %> </td>
								<td> <%= ele.visitHistory.length %></td>
							</tr>
						<% }) %>
					</tbody>
				</table>
			<% } %>
		</div>
	</div>
</body>
</html>